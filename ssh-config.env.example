# Configuration SSH et réseau
# Copiez ce fichier vers .env et modifiez les valeurs

# Adresse IP Tailscale du Raspberry Pi
TAILSCALE_IP=100.x.x.x

# Port SSH (par défaut: 22)
SSH_PORT=22

# Utilisateur SSH
SSH_USER=votre_utilisateur

# Commande de test de connectivité
NC_COMMAND=nc -zv ${TAILSCALE_IP} ${SSH_PORT}

# Commande SSH
SSH_COMMAND=ssh ${SSH_USER}@${TAILSCALE_IP}
